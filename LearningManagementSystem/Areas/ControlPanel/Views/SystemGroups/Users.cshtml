@model DataEntity.Models.EfModels.SystemGroup
@inject IViewLocalizer Localizer
@{
    //DataEntity.Models.EfModels.SystemGroup systemGroup =
    //    (DataEntity.Models.EfModels.SystemGroup)ViewBag.SystemGroup;

    //string[] systemGroupUsers = (string[])ViewBag.SystemGroupUsers;

    ViewData["Title"] = @Localizer["Add Users"];
}

@section Css
{
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/bbbootstrap/libraries@main/choices.min.css">
    <link href="~/css/wizard.css" rel="stylesheet" />
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>@Model.Name <small> @Localizer["Add Users"]</small></h2>
            </div>
            <div class="x_content">
                <form action="/ControlPanel/SystemGroups/Users" method="post" class="form-horizontal form-label-left">
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <input type="hidden" name="id" value="@Model.Id" />

                    @*@foreach (var item in Model)
                        {
                            <div class="form-group">
                                <label class="control-label" style="display:flex;align-items:center; gap:.5rem"><input type="checkbox" name="usernames" value="@item.Username" @(systemGroupUsers.Contains(item.Username) ? "checked" : "") />@item.Username</label>
                            </div>
                        }*@
                    <div style="max-width:25rem">

                        <select name="Updater" class="UpdaterId UpdaterIdValue" id="Updater" multiple></select>

                    </div>
                    <div class="ln_solid"></div>
                    <div class="form-group">
                        <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                            <input type="submit" value=@Localizer["Submit"] class="btn btn-success" />
                        </div>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>


@section JavaScript
{
    <script src="https://cdn.jsdelivr.net/gh/bbbootstrap/libraries@main/choices.min.js"></script>
    <script>
        GetSystemGroupUsers('@Model.Id')
    </script>
}